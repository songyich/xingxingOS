// ============================================================================
//  include/kernel/font.hpp —— 字库接口
//  ---------------------------------------------------------------------------
//  字模数据（g_glyphs）在 kernel/font.cpp 里，由脚本渲染生成。
//  这里只暴露「一个字符宽高多少」和「取某个字符的点阵数据」两个东西。
// ============================================================================
#pragma once

#include <kernel/types.h>

namespace font {

// 字符单元的像素尺寸（含字符之间的自然间距）
constexpr int WIDTH  = 8;
constexpr int HEIGHT = 16;

// 取字符 c 的点阵数据，返回 16 个字节（每行 1 字节，bit7 是最左像素）
const u8* glyph(char c);

}  // namespace font
